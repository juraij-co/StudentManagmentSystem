<?xml version="1.0" encoding="UTF-8"?>

<?import javafx.scene.layout.*?>
<?import javafx.scene.control.*?>
<?import javafx.geometry.*?>

<VBox xmlns:fx="http://javafx.com/fxml" fx:controller="com.juru.TeachersController"
      spacing="15" alignment="TOP_CENTER" style="-fx-padding: 20;">

    <!-- Header Section -->
    <HBox spacing="10" alignment="CENTER_LEFT">
        <Button text="← Back" onAction="#goBack"
                style="-fx-background-color: #95a5a6; -fx-text-fill: white; -fx-font-weight: bold;" />
        <Label text="Teacher Management"
               style="-fx-font-size: 24px; -fx-font-weight: bold; -fx-text-fill: #2c3e50;" />
        <Region HBox.hgrow="ALWAYS" />
        <Button text="+ Add New Teacher" onAction="#showAddTeacherPopup"
                style="-fx-background-color: #2ecc71; -fx-text-fill: white; -fx-font-weight: bold;" />
    </HBox>

    <!-- Search and Filter Section -->
    <HBox spacing="10" alignment="CENTER_LEFT">
        <TextField fx:id="searchField" promptText="Search teachers..." style="-fx-pref-width: 250;" />
        <Button text="Search" onAction="#searchTeachers"
                style="-fx-background-color: #3498db; -fx-text-fill: white; -fx-font-weight: bold;" />
        <Region HBox.hgrow="ALWAYS" />
        <Label text="Filter by:" style="-fx-font-weight: bold;" />
        <ComboBox fx:id="departmentFilter" promptText="Department" style="-fx-pref-width: 150;" />
        <ComboBox fx:id="courseFilter" promptText="Course" style="-fx-pref-width: 150;" />
        <ComboBox fx:id="semesterFilter" promptText="Semester" style="-fx-pref-width: 150;" />
        <Button text="Clear Filters" onAction="#clearFilters"
                style="-fx-background-color: #e74c3c; -fx-text-fill: white; -fx-font-weight: bold;" />
    </HBox>

    <!-- Teachers Table -->
    <TableView fx:id="teachersTable" style="-fx-pref-height: 500; -fx-pref-width: 100%;">
        <columns>
            <TableColumn text="ID" fx:id="colId" prefWidth="60" />
            <TableColumn text="Name" fx:id="colName" prefWidth="150" />
            <TableColumn text="Username" fx:id="colUsername" prefWidth="150" />
            <TableColumn text="Department" fx:id="colDepartment" prefWidth="120" />
            <TableColumn text="Courses" fx:id="colCourses" prefWidth="200" />
            <TableColumn text="Semesters" fx:id="colSemesters" prefWidth="150" />
            <TableColumn text="Actions" fx:id="colActions" prefWidth="150" />
        </columns>
    </TableView>

    <!-- Status Bar -->
    <HBox alignment="CENTER_RIGHT">
        <Label fx:id="statusLabel" text="Ready"
               style="-fx-text-fill: #7f8c8d; -fx-font-style: italic;" />
    </HBox>

</VBox>
